@page "/compoundinterest"



<h1>Compound interest</h1>
<table class="table">
    <tr>
        <th>Inital Principal</th>
        <th><input @bind="Principal"/></th>
    </tr>
    <tr>
        <th>Years</th>
        <th><input @bind="Years"/></th>
    </tr>
    <tr>
        <th>Annual Interest Rate (%)</th>
        <th><input @bind="InterstRate" /></th>
    </tr>
    <tr>
        <th>Total:</th>
        <th> @Total</th>
    </tr>

</table>
<button class="btn btn-primary" @onclick='Calculate'>Calculate</button>


@code
{
    private double Principal { get; set; } = 5000;
    private double InterstRate { get; set; } = 5;
    private int Years { get; set; } = 10;
    private double total { get; set; } = 0;
    private string Total { get; set; }
    private void Calculate()
    {
        var total = Principal * Math.Pow(1 + InterstRate / (1200.0), Years * 12);
        Total = total.ToString("C");
    }



}
